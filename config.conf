# ============================================================
# Telluride Board Flow Configuration
# ============================================================
# Format: KEY=VALUE (no spaces around '=')
# Lines starting with '#' are comments.
# This file is compatible with both Make (-include) and bash (source).

# ------------------------------ Mandatory -------------------

# Path to your design folder (must be pre-compiled)
# e.g. /wrk/xcohdnobkup7/harsallu/sept/sept25_dmha/mllib/internal/demo/bevformer/encoder/telluride/2x4x4/wts_unicast_3inp_dmha_bw_optv0
DESIGN_FOLDER=/wrk/xcohdnobkup2/yugasrid/my_design

# ------------------------------ Optional --------------------

# Custom run ID for tagging runs (default: run_1)
RUN_ID=my_run_1

# Path to Vitis/XBUILD install (leave commented if not needed)
 XBUILD_PATH=/proj/xbuilds/HEAD_daily_latest/installs/lin64/2026.1/Vitis/
